<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.show.showticketingservice.mapper.UserMapper">

    <insert id="insertUser" parameterType="com.show.showticketingservice.model.User">
        INSERT INTO User(id, password, name, phoneNum, email, address)
        VALUES(#{id}, #{password}, #{name}, #{phoneNum}, #{email}, #{address})
    </insert>

</mapper>